# Database Structure

Active reference: `Database_Structure_v4_FINAL.md` — reconciles the
original v3 design with everything actually built, corrected, and
verified across Phase A and Phase B, verified directly against the live
database. Status is tracked in the document's own header.

`Archive/` contains the superseded design-iteration drafts (v1 flat
model, v2 schema restructure, v3 delta, and now v3 itself, superseded by
v4_FINAL) — kept for history, not for reference. Use the active document
only.

Also in this folder (all active references, not superseded):
- `Database_API_Reference.md` — full API/connection surface for the
  schema-based database.
- `Supabase_MCP_Implementation_Notes.md`
- `Template_Migration_Process.md`
- `Client_Onboarding_Sequence_Spec.md` (Phase C)
- `Database_Architecture_Decisions_History.md` — quick-reference record
  of why the database looks the way it does, across both restructures.

Note: the previously-flagged missing `v3 delta` draft has since turned
up and been archived — `Archive/` now contains all three superseded
drafts referenced in `Database_Structure_v4_FINAL.md`'s own supersession
history.
